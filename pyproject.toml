[project]
name = "actuator-xrl8"
version = "0.1.0"
description = "2D actuator for tests with an ultrasound probe"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.1",
    "flask-socketio>=5.5.1",
    "numpy>=2.3.2",
    "requests>=2.32.5",
    "rpi-gpio>=0.7.1",
]
